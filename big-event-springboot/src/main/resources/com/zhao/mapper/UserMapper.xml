<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhao.mapper.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="findByUserName" resultType="com.zhao.pojo.User">
        select * from user where username=#{username}
    </select>
    
    <!-- 根据ID查询用户 -->
    <select id="findById" parameterType="java.lang.Integer" resultType="com.zhao.pojo.User">
        select * from user where id=#{id}
    </select>
    
    <!-- 根据邮箱查询用户 -->
    <select id="findByEmail" parameterType="java.lang.String" resultType="com.zhao.pojo.User">
        select * from user where email=#{email}
    </select>
    
    <!-- 根据手机号查询用户 -->
    <select id="findByPhone" parameterType="java.lang.String" resultType="com.zhao.pojo.User">
        select * from user where phone=#{phone}
    </select>
    
    <!-- 根据ID更新密码 -->
    <update id="updatePasswordById">
        update user
        set password=#{encryptPassword},
            update_time=now()
        where id=#{userId}
    </update>

    <!-- 添加（邮箱方式） -->
    <!-- now()是数据库里面的函数，旨在获取数据库系统当前时间 -->
    <insert id="add" >
        insert into user(username,password,email,create_time,update_time)
        values(#{username},#{encryptPassword},#{email},now(),now())
    </insert>
    
    <!-- 添加（手机号方式） -->
    <insert id="addByPhone" >
        insert into user(username,password,phone,create_time,update_time)
        values(#{username},#{encryptPassword},#{phone},now(),now())
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="com.zhao.pojo.User">
        update user
        <set>
            <if test="nickname != null">nickname=#{nickname},</if>
            <if test="email != null">email=#{email},</if>
            <if test="status != null">status=#{status},</if>
            update_time=#{updateTime}
        </set>
        where id=#{id}
    </update>

    <!-- 更新用户头像 -->
    <update id="updateAvatar">
        update user
        set user_pic=#{avatarUrl},
            update_time=now()
        where id = #{id}
    </update>

    <!-- 更新用户密码 -->
    <update id="updatePwd" parameterType="com.zhao.pojo.User">
        update user
        set password=#{hsPassword},
            update_time=now()
        where id=#{id}
    </update>
    
    <!-- 更新用户角色 -->
    <update id="updateRole" parameterType="com.zhao.pojo.User">
        update user
        set role=#{role},
            update_time=now()
        where id=#{id}
    </update>
    
    <!-- 获取用户列表 -->
    <select id="getUserList" resultType="com.zhao.pojo.User">
        select * from user
        where 1=1
        <if test="role != null">
            and role = #{role}
        </if>
        <if test="username != null and username != ''">
            and username like concat('%', #{username}, '%')
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        <if test="excludeId != null">
            and id != #{excludeId}
        </if>
        order by create_time desc
        limit #{start}, #{pageSize}
    </select>
    
    <!-- 获取用户总数 -->
    <select id="getUserCount" resultType="java.lang.Integer">
        select count(*)
        from user
        where 1=1
        <if test="role != null">
            and role = #{role}
        </if>
        <if test="username != null and username != ''">
            and username like concat('%', #{username}, '%')
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        <if test="excludeId != null">
            and id != #{excludeId}
        </if>
    </select>
    
    <!-- 获取用户总数（数据统计） -->
    <select id="getTotalUsers" resultType="java.lang.Integer">
        select count(*) from user
    </select>
    
    <!-- 获取今日新增用户数 -->
    <select id="getTodayNewUsers" resultType="java.lang.Integer">
        select count(*) from user where create_time >= #{todayStart}
    </select>
    
    <!-- 获取日活跃用户数 -->
    <select id="getDailyActiveUsers" resultType="java.lang.Integer">
        select count(*) from (
            select distinct admin_id as user_id from admin_operation_log where operation_time >= #{todayStart}
            union
            select id as user_id from user where update_time >= #{todayStart}
        ) as active_users
    </select>
    
    <!-- 根据日期范围获取新增用户数 -->
    <select id="getNewUsersByDateRange" resultType="java.lang.Integer">
        select count(*) from user where create_time >= #{start} and create_time &lt; #{end}
    </select>
    
    <!-- 更新用户的更新时间（用于登录时更新） -->
    <update id="updateLastLoginTime" parameterType="java.lang.Integer">
        update user set update_time = now() where id = #{id}
    </update>
</mapper>