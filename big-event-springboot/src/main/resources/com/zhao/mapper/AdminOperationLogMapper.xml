<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhao.mapper.AdminOperationLogMapper">

    <!-- 插入管理员操作日志 -->
    <insert id="insert" parameterType="com.zhao.pojo.AdminOperationLog">
        insert into admin_operation_log(admin_id, operation_content, operation_time, ip_address)
        values(#{adminId}, #{operationContent}, #{operationTime}, #{ipAddress})
    </insert>
</mapper>