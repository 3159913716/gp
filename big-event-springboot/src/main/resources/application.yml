spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/big_event?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0
  mail:
    host: smtp.qq.com
    username: 3486692880@qq.com
    password: 123456
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            enable: true
  spug:
    sms:
      # Spug短信服务API地址
      api-url: ${SPUG_SMS_API_URL:http://your-spug-server/api/v1/sms/send}
      # Spug短信服务密钥
      api-key: ${SPUG_SMS_API_KEY:your-api-key}
      # 短信模板ID
      template-id: ${SPUG_SMS_TEMPLATE_ID:your-template-id}
      # 验证码有效期（分钟）
      code-expiry: 5


  #上传文件限制大小10mb
  servlet:
      multipart:
        max-file-size: 10MB
        max-request-size: 10MB


#    映射下划线到驼峰命名,开启驼峰命名和下划线命名的自动转换
mybatis:
  configuration:
    map-underscore-to-camel-case: true

# Spug短信服务配置
spug:
  sms:
    # Spug短信服务API地址
    api-url: ${SPUG_SMS_API_URL:https://push.spug.cc/send/My5R7m0APMjV2DgG}
    # Spug短信服务密钥
    api-key: ${SPUG_SMS_API_KEY:your-api-key}
    # 短信模板ID
    template-id: ${SPUG_SMS_TEMPLATE_ID:your-template-id}
    # 验证码有效期（分钟）
    code-expiry: 5






